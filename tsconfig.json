{
  "extends": "@vue/tsconfig",
  "files": ["catalog.data.ts", "config.ts"],
  "include": ["plugins/*.ts", "types/*.ts", "constants/*.ts"],
  "compilerOptions": {
    "types": ["node", "markdown-it"],

    /* Linting */
    "noImplicitAny": true,
    "strictNullChecks": true
  }
}
